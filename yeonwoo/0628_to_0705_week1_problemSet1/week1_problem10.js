/**
 * Write a function that generates all combinations of a string
 * keep (with question 29)
 * @param str
 */
function combinations(str) {
    let arr = str.split('')
    for ( let i = 0 ; i < arr.length ; i++) {

    }
}

console.log(combinations('dog'))